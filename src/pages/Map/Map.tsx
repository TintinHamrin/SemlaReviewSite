import React, { useState } from 'react';
import { MapModel } from '../../models/mapmodel';
import './Map.scss';

const Map = () => {
  // const map = new MapModel();
  return (
    <div>
      <h1>karta</h1>
    </div>
  );
};

export default Map;
